import {
  capitalizeWords
} from '../../../helpers/capitalize-words';
import { module, test } from 'qunit';

module('CapitalizeWordsHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = capitalizeWords(42);
  assert.ok(result);
});
